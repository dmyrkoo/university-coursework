name: Check README Workflow
on: [push, workflow_dispatch]
jobs:
  check-readme:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          if [ -f README.md ]; then echo "Success"; else exit 1; fi
